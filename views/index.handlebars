<h1>List Dashboard</h1>
<h2>Here are the available lists:</h2>
<ul>
  {{#each lists}}
    <li><h3>{{ title }}</h3></li>
    <ul>
      {{#each tasks as |task i|}}
          {{> tasks/user-task task}}
      {{/each}}
    </ul>
  {{/each}}
</ul>